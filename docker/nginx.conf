resolver 127.0.0.11 valid=10s;

server {
    listen 80;

    client_max_body_size 50M;
    proxy_connect_timeout 60s;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;

    location / {
        # Using a variable forces nginx to re-resolve "api" via Docker's
        # embedded DNS on every request instead of caching it once at
        # startup, so requests get spread across however many "api"
        # replicas are currently running (docker compose up --scale api=N).
        set $upstream http://api:8000;
        proxy_pass $upstream;
    }
}
